version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
    ports:
      - "{{DEV_PORT}}:8080"
    environment:
      - ENV=development
      - PROJECT_NAME={{PROJECT_NAME}}
