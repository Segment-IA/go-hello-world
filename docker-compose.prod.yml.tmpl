version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "{{PROD_PORT}}:8080"
    environment:
      - ENV=production
      - PROJECT_NAME={{PROJECT_NAME}}
